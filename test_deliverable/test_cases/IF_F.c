int if_false(){
	//int x = 0; think i can get away w no assignment here
	if(0){ // is having an == expression here better?? i dont think its the if statement that tests the functinability of the expression 
		return 1; //idm returning here because its defo wrong				
	} //dont want to return before this line as checking the {} is exited correctly 
	return 0; //is retuning an int to much ??? should i return 0??
}
